const state = {
  name: 'AAA'
}

const getters = {
  nameFirstLetter: (state) => state.name.substr(0,1)
}

const mutaitons = {
  //
}

const actions = {
  //
}

export default {
  state,
  getters,
  mutaitons,
  actions
}
